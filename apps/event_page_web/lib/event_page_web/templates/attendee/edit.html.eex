<h2><%= @event.name %></h2>
<h3>Edit <%= @attendee.name %></h3>

<%= render "form.html", changeset: @changeset, action: attendee_path(@conn, :update, @event.id, @attendee.id) %>

<span><%= link "Back", to: attendee_path(@conn, :index, @event.id) %></span>
